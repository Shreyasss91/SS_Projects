"""Test suites for the Market Depth Recorder. Every test runs without a live broker/WebSocket/market
feed (CLAUDE.md testing rule) — config, utils, and (later) replay determinism against fixtures."""
